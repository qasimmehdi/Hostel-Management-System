@model IEnumerable<HostelManagment.Models.Visitor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@Html.Partial("SearchView")

<p>
    @Html.ActionLink("Create New", "Add")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CNIC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.S_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Visitor_Name)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.Times_Visited)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CNIC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.S_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Visitor_Name)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.Times_Visited)
        </td>
        <td>
            @Html.ActionLink("Edit", "Update", new { id = item.CNIC/* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.CNIC /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
