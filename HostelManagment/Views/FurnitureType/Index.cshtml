@model IEnumerable<HostelManagment.Models.Furniture_type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@Html.Partial("SearchView")

<p>
    @Html.ActionLink("Create New", "Add")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Furniture_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Furniture_Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Furniture_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Furniture_Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Update", new {id = item.Furniture_ID /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Furniture_ID /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
